<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Career Navigator</title>
  <meta name="description" content="Приложение для построения карьерного пути">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  
  <!-- Подключение CSS -->
  <link rel="stylesheet" href="/css/main.css" />
  
  <!-- Заголовки, переданные из сервера -->
  <% if (typeof pageStyles !== 'undefined') { %>
    <style>
      <%= pageStyles %>
    </style>
  <% } %>
</head>
<body>
  <header>
    <nav class="navbar">
      <div class="container">
        <h1>💼 Career Navigator</h1>
        <ul>
          <li><a href="/">Главная</a></li>
          <li><a href="/users">Пользователи</a></li>
          <li><a href="/about">О проекте</a></li>
        </ul>
      </div>
    </nav>
  </header>

  <main class="container">
    <h2>Добро пожаловать в Career Navigator</h2>
    <p>Это платформа для построения карьерного пути и поиска возможностей развития.</p>

    <!-- Пример динамического контента -->
    <% if (users && users.length > 0) { %>
      <ul>
        <% users.forEach(function(user) { %>
          <li><%= user.name %> — <%= user.email %></li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>Нет пользователей.</p>
    <% } %>
  </main>

  <footer>
    <div class="container">
      <p>&copy; <%= new Date().getFullYear() %> Career Navigator. Все права защищены.</p>
    </div>
  </footer>

  <!-- Подключение JS -->
  <script src="/js/main.js"></script>
</body>
</html>
